{
  "$schema": "https://turborepo.com/schema.json",
  "globalPassThroughEnv": [
    "BRAIN_TYPE",
    "AI_URL",
    "AI_KEY",
    "AI_MODEL",
    "TTS_TYPE",
    "TTS_URL",
    "VOXCPM_URL",
    "VOXCPM_VOICE_DESIGN",
    "REDIS_URL",
    "SESSION_IDLE_MS",
    "LONG_TERM_PATH",
    "DATABASE_URL",
    "NCM_COOKIE",
    "USER_PREFS_DIR",
    "MIGRATIONS_DIR",
    "SERVER_PORT",
    "PWA_PORT",
    "NCM_PORT",
    "NODE_ENV"
  ],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
